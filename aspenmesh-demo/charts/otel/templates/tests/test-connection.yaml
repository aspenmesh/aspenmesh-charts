apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "otelCollector.fullname" . }}-test-connection"
  labels:
    {{- include "otelCollector.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "otelCollector.fullname" . }}:{{ .Values.otelCollector.service.port }}']
  restartPolicy: Never
